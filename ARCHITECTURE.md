# 🏗️ Deployment Architecture

## 📊 Architecture Overview

### Option 1: EBS (Local Storage) Architecture
```
┌─────────────────────────────────────────────────────────────┐
│                        AWS EC2 Instance                      │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │              Docker Container                       │    │
│  │                                                     │    │
│  │  ┌──────────────────────────────────────────┐     │    │
│  │  │         Flask App (app.py)               │     │    │
│  │  │         Port 8080                        │     │    │
│  │  └──────────────────────────────────────────┘     │    │
│  │                    ↓                               │    │
│  │  ┌──────────────────────────────────────────┐     │    │
│  │  │    TensorFlow Model + Scaler             │     │    │
│  │  │    (loaded from local disk)              │     │    │
│  │  └──────────────────────────────────────────┘     │    │
│  │                    ↓                               │    │
│  │  ┌──────────────────────────────────────────┐     │    │
│  │  │    CSV Data (EUR/USD prices)             │     │    │
│  │  │    (loaded from local disk)              │     │    │
│  │  └──────────────────────────────────────────┘     │    │
│  │                                                     │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │              EBS Volume (30GB)                      │    │
│  │                                                     │    │
│  │  /app/models/lstm_trained_model.keras              │    │
│  │  /app/models/lstm_scaler.joblib                    │    │
│  │  /app/data/processed/lstm_simple_test_data.csv     │    │
│  │                                                     │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
└─────────────────────────────────────────────────────────────┘
                           ↑
                           │ HTTP (Port 80)
                           │
                    ┌──────┴──────┐
                    │   Internet  │
                    │   Users     │
                    └─────────────┘
```

**Pros:**
- ✅ Simple architecture
- ✅ Fast local disk access
- ✅ No external dependencies

**Cons:**
- ❌ Models tied to instance
- ❌ Can't share across instances

---

### Option 2: S3 (Cloud Storage) Architecture
```
┌─────────────────────────────────────────────────────────────┐
│                        AWS EC2 Instance                      │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │              Docker Container                       │    │
│  │                                                     │    │
│  │  ┌──────────────────────────────────────────┐     │    │
│  │  │      Flask App (app_cloud.py)            │     │    │
│  │  │      Port 8080                           │     │    │
│  │  └──────────────────────────────────────────┘     │    │
│  │                    ↓                               │    │
│  │  ┌──────────────────────────────────────────┐     │    │
│  │  │    TensorFlow Model + Scaler             │     │    │
│  │  │    (cached from S3)                      │     │    │
│  │  └──────────────────────────────────────────┘     │    │
│  │                    ↓                               │    │
│  │  ┌──────────────────────────────────────────┐     │    │
│  │  │    CSV Data (EUR/USD prices)             │     │    │
│  │  │    (cached from S3)                      │     │    │
│  │  └──────────────────────────────────────────┘     │    │
│  │                                                     │    │
│  └────────────────────────────────────────────────────┘    │
│                    ↑                                        │
│                    │ Download on startup                    │
│                    │ (boto3)                                │
└────────────────────┼────────────────────────────────────────┘
                     │
                     │ IAM Role
                     ↓
        ┌────────────────────────────┐
        │       AWS S3 Bucket        │
        │   (eurusd-ml-models)       │
        │                            │
        │  models/                   │
        │    lstm_trained_model.keras│
        │    lstm_scaler.joblib      │
        │  data/                     │
        │    processed/              │
        │      lstm_simple_test.csv  │
        │                            │
        └────────────────────────────┘
                     ↑
                     │
              ┌──────┴──────┐
              │  Versioning │
              │  Backups    │
              └─────────────┘
```

**Pros:**
- ✅ Centralized model storage
- ✅ Easy updates (just update S3)
- ✅ Multi-instance support
- ✅ Automatic backups

**Cons:**
- ❌ Network dependency
- ❌ Slower first startup

---

## 🔄 Request Flow

### EBS Flow
```
User Request
    ↓
Internet (Port 80)
    ↓
EC2 Security Group
    ↓
Docker Container (Port 8080)
    ↓
Flask App (app.py)
    ↓
Load Model from /app/models/ (Local Disk)
    ↓
Load Data from /app/data/ (Local Disk)
    ↓
Make Prediction
    ↓
Return JSON Response
    ↓
User
```

**Latency:** ~50-100ms

---

### S3 Flow

**First Request (Cold Start):**
```
Container Starts
    ↓
Flask App (app_cloud.py)
    ↓
Check USE_S3=true
    ↓
Download from S3 (boto3)
    ├─ models/lstm_trained_model.keras
    ├─ models/lstm_scaler.joblib
    └─ data/processed/lstm_simple_test_data.csv
    ↓
Cache Locally in /app/models/ and /app/data/
    ↓
Load Model from Cache
    ↓
Ready for Requests
```

**Subsequent Requests:**
```
User Request
    ↓
Flask App (app_cloud.py)
    ↓
Load Model from Cache (Local Disk)
    ↓
Make Prediction
    ↓
Return JSON Response
    ↓
User
```

**Latency:**
- First startup: ~15 seconds (download from S3)
- Subsequent requests: ~50-100ms (cached)

---

## 🚀 Deployment Flow

### Local Testing
```
Developer Machine
    ↓
docker-compose up
    ↓
Build Docker Image
    ↓
Run Container (Port 8080)
    ↓
Test at http://localhost:8080
    ↓
Verify Predictions
    ↓
Ready for EC2 Deployment
```

---

### EC2 Deployment (EBS)
```
Developer Machine
    ↓
Create tar.gz (api/ + models/ + data/)
    ↓
SCP to EC2
    ↓
EC2 Instance
    ↓
Extract Files
    ↓
./deploy.sh local
    ↓
Build Docker Image (includes models/data)
    ↓
Run Container
    ↓
App Running on Port 80
```

**Image Size:** ~2GB (includes models)

---

### EC2 Deployment (S3)
```
Developer Machine
    ↓
Upload Models to S3
    ↓
Create tar.gz (api/ only, no models/data)
    ↓
SCP to EC2
    ↓
EC2 Instance
    ↓
Extract Files
    ↓
./deploy.sh s3 bucket-name
    ↓
Build Docker Image (no models inside)
    ↓
Run Container
    ↓
Container Downloads from S3 on Startup
    ↓
App Running on Port 80
```

**Image Size:** ~500MB (no models)

---

## 🔐 Security Architecture

### EBS Security
```
┌─────────────────────────────┐
│    Security Group           │
│                             │
│  Inbound Rules:             │
│  - Port 80 (HTTP)           │
│  - Port 22 (SSH, your IP)   │
│                             │
└─────────────────────────────┘
            ↓
┌─────────────────────────────┐
│    EC2 Instance             │
│                             │
│  - No IAM role needed       │
│  - Models on EBS volume     │
│                             │
└─────────────────────────────┘
```

---

### S3 Security
```
┌─────────────────────────────┐
│    Security Group           │
│                             │
│  Inbound Rules:             │
│  - Port 80 (HTTP)           │
│  - Port 22 (SSH, your IP)   │
│                             │
└─────────────────────────────┘
            ↓
┌─────────────────────────────┐
│    EC2 Instance             │
│                             │
│  IAM Instance Profile:      │
│  - eurusd-ec2-profile       │
│                             │
│  IAM Role:                  │
│  - eurusd-ec2-s3-role       │
│                             │
│  Permissions:               │
│  - s3:GetObject             │
│  - s3:ListBucket            │
│                             │
└─────────────────────────────┘
            ↓
┌─────────────────────────────┐
│    S3 Bucket                │
│    (eurusd-ml-models)       │
│                             │
│  Bucket Policy:             │
│  - Allow EC2 role only      │
│  - Private (no public)      │
│                             │
│  Versioning: Enabled        │
│  Encryption: AES-256        │
│                             │
└─────────────────────────────┘
```

---

## 📊 Scaling Architecture

### Single Instance (EBS)
```
        Internet
            ↓
    ┌───────────────┐
    │   EC2 (EBS)   │
    │   Port 80     │
    └───────────────┘
```

**Capacity:** ~100 requests/second

---

### Multi-Instance (S3 Required)
```
            Internet
                ↓
    ┌───────────────────────┐
    │  Application Load     │
    │  Balancer (ALB)       │
    └───────────────────────┘
                ↓
        ┌───────┴───────┐
        ↓               ↓
┌───────────────┐ ┌───────────────┐
│   EC2 #1      │ │   EC2 #2      │
│   Port 8080   │ │   Port 8080   │
└───────────────┘ └───────────────┘
        ↓               ↓
        └───────┬───────┘
                ↓
        ┌───────────────┐
        │   S3 Bucket   │
        │   (Models)    │
        └───────────────┘
```

**Capacity:** ~200+ requests/second

---

## 🔄 Update Workflow

### EBS Update Flow
```
New Model Trained
    ↓
Replace models/ directory locally
    ↓
Rebuild Docker Image
    ↓
Transfer to EC2
    ↓
./deploy.sh local
    ↓
Downtime: ~2 minutes
```

---

### S3 Update Flow
```
New Model Trained
    ↓
Upload to S3
    ↓
aws s3 cp new_model.keras s3://bucket/models/
    ↓
Restart Container
    ↓
docker restart eurusd-app
    ↓
Downloads new model from S3
    ↓
Downtime: ~15 seconds
```

---

## 💰 Cost Architecture

### EBS Monthly Costs
```
┌─────────────────────────────┐
│  EC2 t3.medium              │  $30.00
│  (2 vCPU, 4GB RAM)          │
├─────────────────────────────┤
│  EBS 30GB gp3               │  $ 3.00
│  (OS + Models + Data)       │
├─────────────────────────────┤
│  Data Transfer              │  $ 1.00
│  (minimal)                  │
├─────────────────────────────┤
│  TOTAL                      │  $34.00/mo
└─────────────────────────────┘
```

---

### S3 Monthly Costs
```
┌─────────────────────────────┐
│  EC2 t3.medium              │  $30.00
│  (2 vCPU, 4GB RAM)          │
├─────────────────────────────┤
│  EBS 10GB gp3               │  $ 1.00
│  (OS only)                  │
├─────────────────────────────┤
│  S3 Storage (5GB)           │  $ 0.12
│  (Models + Data)            │
├─────────────────────────────┤
│  S3 Requests                │  $ 0.01
│  (~10k GET/month)           │
├─────────────────────────────┤
│  Data Transfer              │  $ 1.00
│  (minimal)                  │
├─────────────────────────────┤
│  TOTAL                      │  $32.00/mo
└─────────────────────────────┘
```

---

## 🎯 Recommendation

**For Your Capstone:** Start with **EBS (Local Storage)**
- Simpler architecture
- Easier to explain
- No IAM complexity
- Perfect for demo/portfolio

**Upgrade to S3 when:**
- Moving to production
- Need multi-instance scaling
- Want easy model updates
- Need professional architecture

---

**See other documentation files for implementation details!**
